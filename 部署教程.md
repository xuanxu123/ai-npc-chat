# AI 助手部署教程（完全免费）

## 部署到 Vercel（推荐）

Vercel 提供免费的域名和自动更新，任何人都能通过链接访问。

### 第一步：创建 GitHub 账号（如果没有）

1. 打开 https://github.com/signup
2. 填写邮箱、密码、用户名
3. 验证邮箱
4. 登录 GitHub

---

### 第二步：创建 GitHub 仓库

1. 登录 https://github.com
2. 点击右上角 **+** 号 → **New repository**
3. Repository name：填 `ai-npc-chat`（或其他名字）
4. 勾选 **Public**（公开仓库）
5. 点击 **Create repository**
6. 复制页面上显示的 git 地址，类似：
   ```
   https://github.com/你的用户名/ai-npc-chat.git
   ```

---

### 第三步：把项目推送到 GitHub

1. 打开命令提示符（Win + R，输入 `cmd`，回车）
2. 输入以下命令（一行一行输入）：

```bash
cd D:\game
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/你的用户名/ai-npc-chat.git
git push -u origin main
```

> 注意：把 `你的用户名` 换成你的 GitHub 用户名
>
> 如果推送时要求输入密码，粘贴你的 GitHub Personal Access Token（不是密码）

---

### 第四步：部署到 Vercel

1. 打开 https://vercel.com/new
2. 点击 **Log in with GitHub**（用 GitHub 账号登录）
3. 在 **Add New...** 页面，找到你刚才创建的 `ai-npc-chat` 仓库
4. 点击 **Import**
5. 进入部署配置页面：
   - **Project Name**：随便填，比如 `ai-npc-chat`
   - **Framework Preset**：选择 **Next.js**
   - 点击 **Environment Variables**，添加以下变量：
     ```
     ANTHROPIC_API_KEY = （去 https://console.agnes-ai.com 获取）
     NEXTAUTH_SECRET = （随便填一个随机字符串）
     ```
6. 点击 **Deploy**
7. 等待 2-3 分钟，部署完成后会显示一个链接，类似：
   ```
   https://ai-npc-chat.vercel.app
   ```

---

### 第五步：完成！

把 Vercel 的链接发给别人，他们就能访问了。

---

## 如果遇到问题

### 问题：GitHub 推送失败

```bash
# 检查 git 状态
git status

# 如果有未提交的更改，先提交
git add .
git commit -m "update"

# 再推送
git push -u origin main
```

### 问题：Vercel 部署失败

1. 进入 Vercel 项目页面
2. 点击 **Settings** → **Environment Variables**
3. 确认 `ANTHROPIC_API_KEY` 和 `NEXTAUTH_SECRET` 已经添加
4. 点击 **Deploy** 重新部署

### 问题：API Key 无效

1. 去 https://console.agnes-ai.com 获取新的 API Key
2. 在 Vercel 的 **Settings** → **Environment Variables** 更新
3. 重新部署

---

## 自定义 NPC 设定

如果你想修改 NPC 的说话风格或设定：

1. 编辑项目文件：
   ```
   D:\game\src\app\api\chat\route.ts
   ```
2. 找到 `systemPrompt` 部分，修改你的设定
3. 保存后推送到 GitHub：
   ```bash
   git add .
   git commit -m "update npc settings"
   git push
   ```
4. Vercel 会自动重新部署
