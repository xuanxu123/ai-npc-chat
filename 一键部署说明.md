# 一键部署教程

## 方式一：使用脚本（推荐）

1. 打开命令提示符（Win + R，输入 `cmd`，回车）
2. 复制粘贴以下命令，回车：

```bash
cd D:\game && bash deploy.sh
```

3. 等待脚本执行完成
4. 打开 https://vercel.com/new
5. 点击 **Log in with GitHub**
6. 选择 `ai-npc-chat` 仓库
7. 添加环境变量：
   - `ANTHROPIC_API_KEY` = `YOUR_API_KEY`
   - `NEXTAUTH_SECRET` = `mysecret123`
8. 点击 **Deploy**
9. 等待 2-3 分钟，获得访问链接

---

## 方式二：手动操作

### 第一步：推送代码到 GitHub

1. 打开命令提示符
2. 运行以下命令（一行一行）：

```bash
cd D:\game
rm -f .git/index.lock
git init
git add .
git commit -m "Initial commit"
git remote add origin https://xuanxu123:YOUR_TOKEN@github.com/xuanxu123/ai-npc-chat.git
git branch -M main
git push -u origin main
```

### 第二步：部署到 Vercel

1. 打开 https://vercel.com/new
2. 点击 **Log in with GitHub**
3. 在 **Import** 页面找到 `ai-npc-chat` 仓库，点击 **Import**
4. 进入部署配置页面：
   - **Project Name**：`ai-npc-chat`
   - **Framework Preset**：`Next.js`
   - 点击 **Environment Variables**，添加：
     ```
     ANTHROPIC_API_KEY = YOUR_API_KEY
     NEXTAUTH_SECRET = mysecret123
     ```
5. 点击 **Deploy**
6. 等待 2-3 分钟，部署完成后会显示链接

---

## 部署成功后

Vercel 会给你一个链接，类似：
```
https://ai-npc-chat.vercel.app
```

把这个链接发给任何人，他们都能打开跟你一样的 NPC 聊天界面！

---

## 修改 NPC 设定

如果你想修改 NPC 的说话风格或名字：

1. 编辑文件：`D:\game\src\app\api\chat\route.ts`
2. 找到第 14 行开始的 `systemPrompt`，修改你的设定
3. 保存后运行：
   ```bash
   cd D:\game
   git add .
   git commit -m "update npc"
   git push
   ```
4. Vercel 会自动重新部署
